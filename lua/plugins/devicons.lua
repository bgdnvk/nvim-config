-- lua/plugins/devicons.lua  ----------------------------------------
return {
  "nvim-tree/nvim-web-devicons",
  lazy = true, -- let other plugins load it on demand
  opts = {}, -- default icon set is fine
}
